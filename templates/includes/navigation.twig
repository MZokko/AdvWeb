<div>
  <img src="images/makeup/images.png" class="img-fluid" alt="Responsive image">
</div>
<nav class="navbar sticky-top navbar-light bg-light justify-content-center ">
  
  
  <ul class="nav justify-content-center">
  
      {% for name, link in pages %}
      <li class="nav-item">
        <a class="nav-link text-capitalize {{(link == currentPage) ? 'active' : '' }}" href="{{link}}">{{name}}</a>
      </li>
      {% endfor %}
  </ul>
    
</nav>